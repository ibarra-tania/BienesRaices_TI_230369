extends ../layout/index

block contenido
    .py-10
        h1.text-4xl.my-10.font-foco.text-center Bienes 
            span.font-normal Raíces  
        h2.text-center.text-2xl.font-rubi= pagina

    div(class="bg-babygray shadow py-8 px-4 rounded-lg max-w-md mx-auto") 
        p(class= error ? 'bg-darkblue' : 'bg-babyblue').py-2.px-5.rounded-lg.w-full.font-foco.text-center.text-white.uppercase= mensaje

        if !error
            a(href="/auth/login" class="text-center font-rubi text-sm text-white uppercase mt-10 block") Ya puedes Iniciar Sesión
